<div class="appointment">
  <div class="container">
    <% @schedulers.each do |s| %>
        <p> Staff Name: <%= s.name %> </p>
        <p> Title: <%= s.role %> </p>
        <br>
    <% end %>
    <%= link_to 'Back to Home', root_url %>
  </div>
</div>
